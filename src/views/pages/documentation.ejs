<%- include('../partials/header') %>

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h2>Documentation de l'API</h2>
                </div>
                <div class="card-body">
                    <% if (typeof docs !== 'undefined') { %>
                        <%- docs %>
                    <% } else { %>
                        <p>La documentation est en cours de chargement...</p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>